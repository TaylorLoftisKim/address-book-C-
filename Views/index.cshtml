<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Address Book - Home</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
		<link rel="stylesheet" type="text/css" href="/Content/css/styles.css">
  </head>
  <body>
    <div class="container">
      <div class="jumbotron">
        <h2>Address Book</h2>
        <a href="/add-contact"><h4>Add a new Contact</h4></a>
        <p>Here are all of your current contacts</p>
        <ul>
          @foreach(var contact in Model)
          {
            <li><a href="/@contact.GetId()">@contact.GetName()</a></li>
          }
        </ul>
        <form action="/contacts/clear" method="post">
          <button type="submit" name="button" class="btn-info">Clear All Contacts</button>
        </form>
      </div>
    </div>
  </body>
